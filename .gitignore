# المكتبات المحملة (يتم تحميلها تلقائياً على السيرفر فلا داعي لرفعها)
node_modules/

# ملف البيانات السرية (يحتوي على رابط قاعدة البيانات وكلمات السر)
.env

# المجلد الذي تُخزن فيه الصور المرفوعة (لأنها تتغير وتأخذ مساحة)
uploads/

# ملفات النظام وسجلات الأخطاء
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store